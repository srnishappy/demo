import { Button } from '@/components/ui/button';

const page = () => {
  return (
    <div>
      <Button variant="destructive" size="lg">
        Click me
      </Button>
    </div>
  );
};
export default page;
